# td-flow

## What this is

The **td-flow** framework itself: a minimal, file-based, repo-portable working agreement for solo development. Same shape every project, conversational interface, structured docs in `.td-flow/`, GitHub commits as memory. Slash commands (`/td-flow-init`, `/td-flow-clear`, `/td-flow-complex-clear`, `/td-flow-close`, `/td-flow-refresh`, `/td-flow-mailbox`, `/td-flow-health`, `/td-flow-incident`, `/td-flow-park`, `/td-flow-snapshot`) — everything else is conversational.

## Who it's for

Solo developers working with Claude Code. Originally built for one user's portfolio; anyone can fork it for their own.

## Stack & choices

- Markdown-only — no compiled code, no npm package, no CLI binary.
- Bash for `install.sh` and the pre-commit hook.
- AWK for extracting the test command from `WORKWAY.md` § Local testing.
- Cross-project requests ride on GitHub Issues + `gh` CLI. No custom DB, no schema, no inbox service.
- **Tracker-free outbound.** Per-project `.td-flow/PROJECT.md § Cross-repo` lists connected repos; `**From:** <project>` body marker on every cross-repo filing identifies the source. `/td-flow-mailbox` does one bounded search across declared repos — no separate registry repo, no tracker Epic, no in-repo tracking infrastructure beyond the human-curated list and the body marker.
- Cloned to `~/projects/td-flow/` on each machine.
- Distributed as symlinks into `~/.claude/` — `commands/` and `td-flow-contract.md` (the contract every project `@import`s) — via `install.sh`; `templates/` is read directly from the repo clone (`~/projects/td-flow/templates/`), no longer symlinked. (No `skills/td-flow` since v6.1, no `td-templates` symlink since the templates-read-from-repo change — see § Shipped.)

## Active scope

(none — v7.5 shipped 2026-05-28 (contract tightening + Bug #14 fix; see § Shipped); v7.4 shipped 2026-05-27 (post-v7.x polish: saved-starter template functionality removed entirely; two new contract principles — § Presenting decisions + extended § Action over questions; mailbox confirm round reversibility-gated; see § Shipped); v7.3 shipped 2026-05-27 (framework repo dogfoods its own pre-commit hook); v7.0–7.2 shipped 2026-05-26. Framework is in a settled lean state. See § Shipped for the full v5.x→v7.x arc. Ongoing: real-project use across the portfolio. Per-machine migration: `cd ~/projects/td-flow && git pull && ./install.sh` — install.sh prints any of the v6.0/v6.1/v7.0 banners that still apply on that machine, then writes the v7.0 ack marker so future installs are silent. Per-project migration: `/td-flow-refresh` from inside each td-flow project — Step 1 migrates a legacy full-copy `CLAUDE.md` onto the `@import` (pre-v5.0 projects only), Step 1.5 prunes deprecated nudge phrasing, **Step 1.7 migrates `.td/` → `.td-flow/` + creates the compat symlink** (pre-v7.0 projects). The framework's `hooks/pre-commit` and `scripts/smoke.sh` carry a `.td/` fallback as the transition safety net so pre-migration projects don't break; the fallback is removed in v8.0.)

## Shipped

- v1 (initial scaffold, 10 slash commands, BIG/SMALL split)
- v2 (collapsed to 7 commands, 6-phase cycle, locked TESTING.md sections)
- v3 (3 slash commands: init/ship/close; conversational interface; WORKWAY.md folds testing+env+frameworks; multi-stack; GSD legacy migration; uncapped Resume note)
- v3.1 (renamed `/td-ship`→conversational; `/td-flow-close` split into `/td-flow-clear` (mid-project handoff) and `/td-flow-close` (project/phase wrap))
- v3.2 (sharpened "Who does what"; added first-message warm-up nudge; added Drift signals section; SKILL.md slimmed to thin pointer; install.sh prunes stale command symlinks)
- v3.3 (added Fold-and-delete rule for `work/<topic>.md` scratch; added "Digging into history" section with git log recipe)
- v3.4 (made the four bypassed rituals explicit: "lets do it" as meaningful-work trigger, "Before I commit a piece" pre-ship + STATE-update + fold-and-delete bundle, and `/init` never-run warning)
- v3.5 (BACKLOG/PROJECT cleanup: dropped auto-test-suite item; first real-project validation set as next-session move; explicit self-validation rationale via drift signals + Before-I-commit bundle)
- v3.6 (td-bus shipped: opt-in cross-project messaging on a shared Turso/libsql DB. Single-file Python CLI, schema, `/td-bus-init` slash command, installer wired `~/bin/td-bus`. **Retired same day in v3.7** after reviewing GitHub Projects v2 / Issues — reinventing GH Issues for a solo dev violated CLAUDE.md's own "GitHub is the memory. Don't duplicate." principle.)
- v3.7 (td-bus retired in favor of GitHub Issues + per-project `## Cross-repo` registry in `.td-flow/PROJECT.md`. Deleted bus CLI, schema, slash command, and `install.sh` blocks. Added cross-repo workflow + warm-up `gh issue list` to root + template `CLAUDE.md`. `## Cross-repo` is opt-in per project — no scaffold. Inbox-scope guardrail: current repo by default, cross-repo opt-in via explicit triggers. Identity-agnostic queries — REPO is the unit, not GH user.)
- v3.8 (public/private split: SERVICES.md (and future user-specific data) moved out of this repo into a private companion registry repo discovered via `$TD_REGISTRY`. The framework repo flipped back to public so the methodology can be shared/forked; user data stays private. Pattern: public framework + per-user private registry.)
- v4.0 (public-identity milestone: GH repo renamed `mergodon/td-nopara` → `mergodon/td-flow`; local clone path renamed `~/projects/td/` → `~/projects/td-flow/`. Friendly name, project name, GH slug, and local path now all line up. Doc refs reconciled across `td-flow` + `td-registry`; downstream-project reconcile guidance updated in README. No surface change to commands, skill, or contract.)
- v4.1 (`/td-flow-mailbox` shipped + tracker model proposed-then-reverted in one day; landed on minimum-dependency design: per-project `.td-flow/PROJECT.md § Cross-repo` list bounds the outbound search, `**From:** <project>` body marker identifies our filings, sub-issues stay for real planning Epics. Plus: mechanical stack-reality-check + doc hygiene at `/td-flow-clear` and `/td-flow-close`; close-as-stale recommendation for outbound; `Feature` Issue Type retired; **`$TD_REGISTRY` private companion registry concept retired entirely** — the friendly-name lookup collapsed to PROJECT.md H1 → directory basename, no separate registry repo needed. Net effect across these changes: 8 slash commands → 7, materially simpler model.)
- v4.2 (ARCHITECTURE.md added as sixth standard `.td-flow/` doc — rationale-focused, not structural. Hooks across the lifecycle: `/td-flow-init` scaffolds it, `/td-flow-clear` heads-up on drift, `/td-flow-close` hygiene-pass review, `/td-flow-refresh` Phase 4 existence check, `/td-flow-incident` close-out captures architectural learnings. Plus: `/td-flow-mailbox` gained `start` (inbound) verb to activate an issue as the current Topic with auto `Closes #N` staging; `/td-flow-clear` + `/td-flow-close` gained mailbox awareness (snapshot + open-Bug/Task gate). Then a same-session simplification pass: dropped `/td-flow-mailbox` outbound `status` sub-menu, consolidated `/td-flow-clear` heads-ups into one block, collapsed `/td-flow-incident` close-out's 3 prompts into one prefix-routed capture, fixed `/td-flow-refresh` Step 1 short-circuit bug, halved CLAUDE.md slash-commands list, restructured CLAUDE.md § Cross-repo for sharper structure. Explicit user call: `templates/CLAUDE.md` and root `CLAUDE.md` kept conceptually distinct — no symlink, mirrored edits per session.)
- v4.3 (framework self-update: `/td-flow-close` gained Step 11 — a read-only check after a successful close that nudges if the local td-flow repo is behind `origin/main`, never pulls. `/td-flow-refresh` gained Phase 0 (new Step 0) — syncs the framework *before* the project refresh: re-runs `install.sh` unconditionally (idempotent — catches stale symlinks independent of repo state), offers a confirm-first `--ff-only` pull if behind. Both resolve the framework repo via the running command's symlink target — clone-path-independent. Design split: detect-at-close, act-at-refresh — don't collapse it: auto-pulling at close, or auto-updating without confirmation, reintroduces the dirty-tree and mid-close-lifecycle hazards the split exists to avoid. Triggered by a real miss this session — `/td-flow-incident` was absent from `~/.claude/commands/` because `install.sh` hadn't been re-run after the command was added.)
- v4.4 (ARCHITECTURE.md removed as a standard `.td-flow/` doc — reverts v4.2. An audit found it ~95% redundant: system shape, key components, and most "important decisions" were already in PROJECT.md § Stack / § Shipped, CLAUDE.md § Cross-repo, and WORKWAY § Framework specifics — and the doc was never in the every-session read set, so it rarely fired. Removed: the template, the `/td-flow-init` scaffold, the `/td-flow-clear` drift heads-up, the `/td-flow-close` hygiene walk, `/td-flow-refresh` Phase 4's ARCHITECTURE branch, the `/td-flow-incident` `arch:` capture route, and the same-session `feat(architecture-draft)` drafting feature (shipped + reverted same day — the td-bus pattern). Standard set back to four docs: PROJECT/WORKWAY/STATE/BACKLOG, plus opt-in DEBUG. Decision rationale lives where it always effectively did — `git log` + PROJECT.md § Shipped. The one load-bearing guard the doc held that wasn't already elsewhere — "don't collapse detect-at-close/act-at-refresh" — folded into the v4.3 entry above. Follow-up: with its ARCHITECTURE branch gone, `/td-flow-refresh` Phase 4 (the standard-docs existence check) was near-vacuous — dropped entirely, taking `/td-flow-refresh` from five phases to four.)
- v4.5 (batch-decide interaction model — from a framework smell review. The framework's serial "walk one item at a time" interaction — `/td-flow-mailbox`'s two issue walks, `/td-flow-park`'s line-by-line BACKLOG walk, `/td-flow-close`'s leftover-parking, `/td-flow-refresh`'s section + cross-repo-drift walks — replaced by a uniform **gather → digest → one decision point → batch execute** shape: present every item at once with a recommended action each, take the user's decisions in a single reply, drill into any item on request (`show N`), execute the batch. `/td-flow-park` gained a **consolidation pass** — related/duplicate BACKLOG lines merge into one issue, visibly and vetoably, instead of a blind 1:1 line→issue mapping — and became the **single canonical BACKLOG-flush procedure**: `/td-flow-close` Step 3 and `/td-flow-refresh` Phase 2 now reference it instead of re-describing it (~40 lines of triplication removed). `/td-flow-mailbox`'s recommendation ladders and staleness heuristics kept verbatim — only the interaction shape changed (trimming the magic-number thresholds was a declined option). `/td-flow-incident` and `/td-flow-clear` untouched — neither walks a list, and `/td-flow-clear` is meant to stay fast.)
- v4.6 (`/td-flow-health` — proactive production health check, the eighth command and the proactive twin of `/td-flow-incident`. A thin generic orchestrator over a project-owned `.td-flow/health.sh` script: the only fixed contract is the protocol — exit `0`/`1`/`2`, `OK`/`WARN`/`FAIL` lines — so the command hardcodes no checks and each project owns its own battery. Read-only on production by design. First run scaffolds the script (drafted from `WORKWAY § Live` + `PROJECT § Stack`, template at `templates/td/health.sh`) or marks the project non-production via an opt-in `PROJECT.md § Health` section. WARNs offer a park-to-BACKLOG; FAILs offer escalation into `/td-flow-incident` with the failing check pre-filled. Single-project scope by design — no cross-project sweep. Modelled on `rgb-rgbtracker-web`'s hand-rolled `.td-flow/ops/health.sh`, generalised into the framework.)
- v4.7 (the ripple-check gate — a new `CLAUDE.md` section, `## The ripple check`, sitting between "Before I commit a piece" and "Drift signals I surface". A change ripples to every doc that *states* or *depends on* the fact it changes; before shipping any commit — `feat`/`fix`/`docs`/`chore`, no exemptions — read the whole-surface docs (`README.md` + the `.td-flow/` docs), trace each changed fact to everywhere else it lives, fix every stale spot in the same commit. Born from a real v4.6 miss: the single-project scoping commit grepped `fleet|sweep|--all|verdict` and missed the README's worked example, which displayed the *removed* verdict-line format without containing any of those keywords — grep catches literal matches, only reading catches semantic ones. That stale README example fixed in the same commit. Mirrored into `templates/CLAUDE.md`.)
- v4.8 (removed the pre-work ride-along nudge — the `## Nudges I do without being asked` line that fired **"Before I dive in, anything else on your mind that should ride along?"** before a scope-open piece — from `CLAUDE.md` and its `templates/CLAUDE.md` mirror. The problem was shape, not frequency: as a turn-ending question it was indistinguishable from a "task done, waiting for input" message, so the user couldn't tell a finished piece from a pre-work scope check. The existing skip-conditions ("skip when the piece is small / scope already settled") never addressed that ambiguity. Considered softening it to non-blocking narration — state-and-proceed instead of ask-and-wait — but the user chose outright removal. The other four nudges in the section — fresh-session STATE summary, "we're scattered — want to wrap?", `/td-flow-clear` and `/td-flow-close` suggestions — left intact; none is a turn-ending question that mimics completion.)
- v5.0 (the contract is delivered by `@import`, not copied per project — the biggest structural change since the conversational rewrite. Root diagnosis: every td-flow project carried a full copy of the ~200-line contract in its `CLAUDE.md`; copies drift, and `/td-flow-refresh` plus a mid-arc managed-file model (a `td:custom` editable region + a take-canonical-and-resplice reconcile) were all machinery to manage that copy — the framework breaking its own "GitHub is the memory. Don't duplicate." rule. v5.0 removes the copy: `install.sh` links `~/.claude/td-flow-contract.md` → the repo's canonical `CLAUDE.md`, and a project's `CLAUDE.md` is now a one-line `@~/.claude/td-flow-contract.md` import. One source of truth, zero per-project drift — `git pull` the framework and every project is current next session. `/td-flow-refresh` shrank to: pull + re-run `install.sh`, plus a one-time migration of a legacy full-copy `CLAUDE.md` onto the import. `templates/CLAUDE.md` collapsed from a full ~200-line mirror to a 3-line import template — the root↔template mirroring chore is gone. The Claude Code `@import` mechanic was proven live before migrating (a throwaway project; `/memory` showed the contract `@-imported`). Net −276 lines. Surviving from the same arc, not superseded: GSD legacy-migration dropped from `/td-flow-init` (long-dead code); unprompted inbox nudges scoped to Bugs/Tasks only — Ideas/Epics surface on request, not between tasks; Idea→Task promotion (`/td-flow-mailbox` `promote`, auto-promote when work starts on an Idea, the "show me the ideas" route); `/td-flow-clear` gained a session doc-sync so `PROJECT`/`WORKWAY` reflect the session before `/clear`. The managed-file model was itself explored and shipped mid-arc, then superseded once the deeper question surfaced — "why is there a per-project copy at all?" Distribution stays clone + `install.sh`: the Claude Code plugin system was evaluated and ruled out — see § Out of scope.)
- v5.1 (full command-surface validation + hardening. All 8 slash commands exercised against real fixtures in three structured tests: a `/td-flow-init`+`/td-flow-refresh` role-play UAT, a cross-repo test on a new kept two-repo harness (`mergodon/td-flow-test1`+`test2` — see WORKWAY § Notes), and a connected `/td-flow-incident`→`/td-flow-health`→`/td-flow-clear`→`/td-flow-close` lifecycle test. Five procedure bugs found and fixed: (1) `/td-flow-init` Step 6 and `/td-flow-refresh` Step 1.3 — the framework's own doc-only `chore:`/`docs:` commits were blocked by the pre-commit hook the framework itself installs; both now commit `--no-verify`. (2) `/td-flow-park` Step 5 dedupe ran one `gh issue list --search` per proposed issue — N serial network calls (3.9s for 6) → now one GraphQL query, matched locally. (3) `/td-flow-mailbox` Step 2 gained a sub-issue rollup-lag note. (4) `/td-flow-incident` Step 6(a) numbered commit before the STATE reset + work-file fold, contradicting its own "same commit" Rule → reordered, commit last. (5) `/td-flow-clear` synced docs + rewrote STATE then pushed with no commit step → added the `chore: clear <topic>` commit. Cross-repo machinery (`**From:**` marker, inbound/outbound walks, `addSubIssue`, Idea→Task promotion) and lifecycle file-handoffs all verified. One reported `/td-flow-init` `.env.example` gap was a false positive — templates ship the file.)
- v5.2 (resumable pause — `/td-flow-snapshot` shipped as the ninth slash command and the foundation for clean mid-flight pivots. Mechanic: commit current state (uncommitted edits + work file) to `snapshot/<slug>` branch, file a `Snapshot`-type GH issue whose body opens with the literal `claude --resume <session-id>` line (using Claude Code's native session resume — no bespoke save-state magic), reset STATE to idle on `main`. Resume = `git checkout snapshot/<slug>` + the resume line. Snapshot Issue Type added to the mergodon org. Also: `/td-flow-incident` rewritten as a `/td-flow-snapshot` composition — Step 1 invokes snapshot when STATE.Topic isn't idle, replacing the bespoke "preserve previous topic as a pointer in Resume note" mechanic that caused #11's failure mode (pointers lose content; branches don't). `/td-flow-mailbox` gained a dedicated Snapshots bucket below the inbound list, with `resume <N>` / `delete <N>` actions, never numbered with inbound/outbound (snapshots are personal lifecycle markers, not work requests). Contract Fix D: the "materialise topic to disk before designing" rule — any multi-step plan must write `.td-flow/work/<slug>.md` *before* the design conversation continues, so a context switch never loses the spec. #11 closed by the rewrite.)
- v5.3 (pre-ship sanity automation. `scripts/smoke.sh` automates the WORKWAY § Local testing pre-ship checklist — bash syntax on `install.sh` + `hooks/pre-commit`, `install.sh` idempotency (two-run), all 9 slash-command symlinks resolve, skill + templates + contract symlinks resolve, AWK extractor in `hooks/pre-commit` returns a non-empty value from `.td-flow/WORKWAY.md § Local testing`. Output protocol mirrors `/td-flow-health`: OK/WARN/FAIL lines, exit 0/1/2. Wired as the WORKWAY Test command so the pre-commit hook runs it on every commit — the framework's first real automated gate. Critical ordering: symlink checks run BEFORE `./install.sh` idempotency (caught by negative-path test: install.sh self-heals symlinks, so running it first masks any broken state). Semantic UAT (does `/td-flow-snapshot` actually work?) stays manual, against `mergodon/td-flow-test1`. Same session: v5.2 UAT pass — `/td-flow-snapshot` standalone, `/td-flow-incident` composition, `/td-flow-mailbox` Snapshot bucket all verified working; surfaced one procedural-clarity bug in `commands/td-flow-snapshot.md` Step 1 (`<original-topic>` / `<original-phase>` / `<original-last>` referenced in later steps but never captured explicitly in Step 1 — fixed). Plus surface-wide doc sweep: README repo layout + intro brought current with all 9 commands and `scripts/`, contract sharpened with the park-vs-snapshot routing distinction, `/td-flow-init` Step 7 highlights the `@import` design + lists commands, `/td-flow-refresh` Step 2 surfaces available commands after install.sh re-link.)
- v6.0 (**breaking rename**: all slash commands renamed from `/td-*` to `/td-flow-*` — `/td-init`→`/td-flow-init`, `/td-clear`→`/td-flow-clear`, `/td-complex-clear`→`/td-flow-complex-clear`, `/td-close`→`/td-flow-close`, `/td-refresh`→`/td-flow-refresh`, `/td-mailbox`→`/td-flow-mailbox`, `/td-health`→`/td-flow-health`, `/td-incident`→`/td-flow-incident`, `/td-park`→`/td-flow-park`, `/td-snapshot`→`/td-flow-snapshot`. Friendly name, project name, GH slug, local path, contract file, skill, and now slash commands all carry the `td-flow` prefix — namespace-consistent across the surface. Clean break, no aliases: `install.sh`'s stale-symlink pruner auto-removes the old names on first `/td-flow-refresh`; install.sh then prints a one-time v6 rename banner with the old→new mapping (only fires when old-name symlinks were detected before install — fresh installs see nothing). 10 command files `git mv`'d to preserve history; 262 `/td-X` references bulk-replaced across commands, contract, skill, README, FEEDBACK, templates, install.sh, and `.td-flow/` docs (perl `-i` with command-specific patterns; `/td-complex-clear` replaced before `/td-clear` to avoid the `/td-complex-flow-clear` collision). Incidental cleanup folded in same commit: `/td-flow-complex-clear` (landed post-v5.3 but never counted in the canonical "Nine commands" anywhere) folded into the contract's command list, the skill, and PROJECT.md intro; framework now correctly self-describes as **Ten slash commands**. (`scripts/smoke.sh § EXPECTED_COMMANDS` was registered in concurrent commit b23a488 — `/td-flow-complex-clear` joins it with the renamed slug here.) Rebased on top of two concurrent post-v5.3 fixes pushed mid-session: b23a488 (`/td-flow-complex-clear` resume-note "Resume — start here" lead block; the bottom-buried first-action pointer was skipped on resume — surfaced by garmin's maiden run) and 85fe620 (`hooks/pre-commit` Test-command trim: `| xargs` stripped quotes around Xcode/JVM-style args; replaced with quote-preserving `sed`, mirrored in `scripts/smoke.sh`). Migration is one command per machine: `cd ~/projects/td-flow && git pull && ./install.sh` (or `/td-flow-refresh` from any td-flow project, which composes both).)
- v6.1 (**td-flow skill retired**: deleted `skill/SKILL.md` and the `skill/` directory; removed the skill install step from `install.sh`; removed the `skills/td-flow` symlink check from `scripts/smoke.sh § 3`. Why useless: the skill duplicated the rhythm + file structure + command list that the contract already covers via the `@import` mechanism every td-flow project carries in its `CLAUDE.md`. The skill's only job ("surface the rhythm when context is heavy or the contract isn't loaded yet") was made redundant by v5.0 when the contract switched from per-project copy to canonical `@import` — the skill was vestigial for ~half a dozen versions before this cleanup. Visible cost it was imposing: `/td-flow` (with no suffix) appeared in Claude Code's slash-command autocomplete next to the 10 real commands `/td-flow-init`, `/td-flow-clear`, etc. — typing `/td-flow` invoked the skill, surfacing its `SKILL.md` body, which read as "what does this do?" confusion. The contract has zero references to the skill, confirming the orthogonal-and-retire-able assessment. `install.sh` gained a v6.1 retirement-detection block + one-time banner mirroring the v6.0 rename pattern: detects pre-existing `~/.claude/skills/td-flow` symlink before the prune step removes it, prints a clear retirement notice only when detected. Fresh installs see nothing. Doc surface swept: README install + repo layout, PROJECT.md § Stack, WORKWAY.md § Local testing checklist + § Live deploy list + § Claude Code skill/commands renamed to § Claude Code commands + contract. STATE.md and PROJECT.md historical entries (e.g. v3.2 "SKILL.md slimmed", v5.0 "skill" mention, v5.3 "skill + templates + contract symlinks resolve", v6.0 "contract file, skill, and now slash commands all carry the td-flow prefix") left intact — those are accurate history at the time, not stale facts.)
- v7.0 (**state dir renamed `.td/` → `.td-flow/`**: the last spot where the framework's name didn't carry the `td-flow` prefix. Post-v6.x: project name = `td-flow`, GH slug = `mergodon/td-flow`, local clone = `~/projects/td-flow/`, contract file = `td-flow-contract.md`, slash commands = `/td-flow-*` — and now per-project state dir = `.td-flow/`. Whole surface namespace-consistent. **Symlink-first migration** (user-proposed safety pattern): `/td-flow-refresh` gained Step 1.7 — `git mv .td .td-flow` (history preserved) + creates `.td → .td-flow` compat symlink so any user-side hardcoded `.td/` refs (IDE bookmarks, custom scripts, `.gitignore` entries) keep resolving. Idempotent: skips already-migrated projects (where `.td/` is a symlink); aborts on conflict (both dirs present as real). Framework-side **safety net**: `hooks/pre-commit` and `scripts/smoke.sh` prefer `.td-flow/` but fall back to `.td/` so projects that pull the framework v7.0 but haven't run `/td-flow-refresh` yet don't break — removed in v8.0 once the portfolio has migrated. `/td-flow-init` now scaffolds `.td-flow/` (template moved from `templates/td/` → `templates/td-flow/` via `git mv`) and creates the compat symlink in new projects. `install.sh` gained a v7.0 detection + one-time banner using a marker file (`~/.claude/.td-flow-v7-acked`): fires once per machine on first install post-v7.0 if a contract symlink existed (signals upgrade, not fresh install); marker created at end of every run so future installs are silent. Pre-rename audit caught one missed file: `hooks/pre-commit` (no `.sh` extension) was skipped by both the v6.0 and v7.0 perl bulk replaces — rewritten by hand to use the new path with fallback and to mention `/td-flow-init` instead of the long-retired `/td-init`. 262 `.td/` references bulk-replaced across all `.md`/`.sh` files except the work spec and the legacy v1/v2 source paths in `/td-flow-init` Step 0 (those describe migration FROM the old dir name, must stay `.td/`). Doc surface swept: README install + commands table + repo layout, WORKWAY.md § Local testing + § Live, PROJECT.md intro + § Stack, all 10 command files updated path refs, templates dir renamed. Live install + smoke clean on this machine after migration (`.td-flow/` is real, no symlink kept in framework repo since we control all references).)
- v7.1 (**smoke gains 3 structural checks — drift catchers triggered by an audit of `/td-flow-complex-clear`'s onboarding**: complex-clear had been a 10th command file for some time before any canonical surface acknowledged it as "the 10th" — caught and fixed during v6.0 cleanup, but the *underlying drift class* had no mechanical detector. Added three checks to `scripts/smoke.sh`: (#6) **cross-reference** — every `commands/td-flow-*.md` listed in `EXPECTED_COMMANDS` must also appear in `CLAUDE.md`'s "Ten commands" trigger map, README's install symlinks list, AND README's slash commands table; would have caught complex-clear's "added but never folded into canonical surfaces" drift retroactively. (#7) **frontmatter validity** — every command file has YAML frontmatter with a non-empty `description:` field; Claude Code uses the description for autocomplete + skill detection, missing it silently breaks discovery. (#8) **complex-clear structural anchors** — assert `# Step 7.5 — Self-validation gate` header + `Resume — start here` lead-block requirement both still present; if a future edit silently drops either, smoke fails (the lead block was b23a488's fix for garmin's resume skipping the bottom-buried first-action pointer — a load-bearing piece, not boilerplate). Smoke now reports 10 OK on clean state, up from 7. Net: drift bugs that previously waited for human audit (or never caught) now fail the pre-commit hook on the spot. Additive change, no breaking surface — projects don't need a refresh for this; framework smoke runs in this repo only.)
- v7.2 (**per-command anchors expanded from complex-clear-only to all 10 commands**: smoke check #8 refactored from a one-command structural test into a generic data-driven check that asserts 2–4 load-bearing anchors per command — 31 anchors total. Pattern is `<command>|<grep pattern>|<short description>` rows in a single bash array; the loop runs one `grep -qE` per row, fails per missing anchor with a specific actionable message. Coverage per command (most load-bearing only — not cosmetic, not over-tested): /td-flow-init (Step 0 detection branch, v7.0 compat symlink scaffold, `@import` target); /td-flow-clear (Step 6 STATE handoff header, `chore: clear` commit convention); /td-flow-complex-clear (Step 7.5 self-validation gate, `Resume — start here` lead block — same two as v7.1); /td-flow-close (Step 11 framework-update check from v4.3, `chore: close` commit, `BACKLOG-flush procedure` delegation to /td-flow-park from v4.5); /td-flow-refresh (Step 0 sync, Step 1.5 nudge prune, Step 1.7 v7.0 dir migration, `--no-verify` for doc-only commits, `--ff-only` for safe framework pulls); /td-flow-mailbox (`**From:**` body marker, `sub_issues` GraphQL header, `Cross-repo` registry); /td-flow-health (`OK/WARN/FAIL` protocol, `= all OK` exit-0 contract line, `.td-flow/health.sh` script path, `/td-flow-incident` escalation); /td-flow-incident (snapshot composition, `STATE.Topic` handling, read-only production constraint); /td-flow-park (consolidation pass from v4.5, `createIssue` GraphQL mutation, `BACKLOG.md` source); /td-flow-snapshot (`snapshot/` branch pattern, `claude --resume` line, `Snapshot-type` issue filing). Two pattern bugs surfaced + fixed during the build: bare `--no-verify` / `--ff-only` patterns were interpreted by grep as option flags (fixed with `grep -qE -e "$pattern"` so `-e` separates options from pattern); `exit 0/1/2` literal anchor for health didn't match the actual prose phrasing (replaced with the unambiguous `= all OK` substring from the protocol-contract line). Smoke output stays at 10 OK (same line count, broader coverage in one OK). Net: every command's load-bearing structural pieces now mechanically gated by the pre-commit hook — a silent regression that drops e.g. /td-flow-refresh's Step 1.7 migration or /td-flow-snapshot's resume-line emission fails on the spot. Adding new anchors as the framework evolves is a one-line array entry.)
- v7.3 (**framework repo dogfoods its own pre-commit hook**: caught during a `/td-flow-refresh` test — this repo's `.git/hooks/pre-commit` had been stale since May 3 (pre-v6.0), so the v6.0 `/td-init` → `/td-flow-init` reference change and 85fe620's xargs→sed quote-preservation fix were sitting in `hooks/pre-commit` (canonical) but never reaching commits to this repo. Manual `cp hooks/pre-commit .git/hooks/` was the only path until now. Fix has two pieces: (a) **`install.sh` Step 6** — `install.sh` always runs from the framework repo, so on every install it now `cmp -s`-checks `hooks/pre-commit` vs `.git/hooks/pre-commit`, copies + `chmod +x` if they differ. Silent no-op on subsequent runs from a synced state. Consumer projects unaffected (their `install.sh` motion is unchanged; `/td-flow-init` Step 5 still handles their hook). (b) **smoke check #3a** — fail-fast drift catcher that asserts `cmp -s hooks/pre-commit .git/hooks/pre-commit` BEFORE check #4 (`install.sh` idempotency) so the drift surfaces as a clear "you edited the canonical hook without running install.sh" message instead of being silently self-healed by check #4's `./install.sh` run. Both pieces together: edit → smoke FAILs with cause + install.sh self-heals on same run + next smoke run is clean. Smoke now reports 11 OK on clean state, up from 10. WORKWAY.md pre-ship list updated; STATE.md side-finding "side-finding (unaddressed): td-flow's own .git/hooks/pre-commit is not installed" cleared. Issue #12 (Idea — branch-awareness) parked in the same session.)
- v7.5 (**contract tightening + Bug #14 fix**. (a) Post-v7.4 content-review on CLAUDE.md — triggered by the user noticing `~/.claude/td-rider-contract.md` is over the 40k warning, asked to apply the same lean-and-loaded-only-if-needed audit here. Contract was 210 lines / 23k chars — under threshold, lean by structural standards, so the pass tightened within the existing shape rather than splitting to skills/rules (the docs-validated bigger conversation: `@import` doesn't reduce context cost, only Skills + `.claude/rules/` do; saved as reference memory for the future split). Trimmed three oversized `## Where things go` router entries (procedural detail lives in `/td-flow-park` Step 5, `/td-flow-snapshot`, and `## Cross-repo § Filing workflow` — entries now point), deduped 3 BACKLOG flush mentions to 1, compressed "Never run /init" 3 sentences → 1, made "Ten commands" count-free (ripple: PROJECT.md "Ten slash commands" → "Slash commands"; WORKWAY's "CLAUDE.md's 'Ten commands' trigger map" → "`## The slash commands` trigger map"), dropped WORKWAY's "11 OK on clean state" magic number. Net CLAUDE.md −1,413 chars / −6% within preserved spirit. Three review findings tagged [verify] / [extend] / [skip] left for explicit user direction (multi-line Action-over-questions principle, contract-internal anchors for smoke §8, smoke meta-test harness). (b) Bug #14 fix (filed by tasmanvisa-web 2026-05-27): pre-commit hook `eval`s the awk-extracted Test command; command substitution fires if WORKWAY's `Test command:` line uses markdown backticks. Three fixes proposed in the body; chose option 1 (strip outer backticks in extractor) — option 2 (`bash -c` instead of `eval`) was misdiagnosed in the bug body (same outcome, both re-parse as shell), option 3 (document the constraint) was tasmanvisa-web's local workaround (discipline not engineering). Extended the sed pipeline in both `hooks/pre-commit` and `scripts/smoke.sh` (they share the extractor verbatim) from `[[:space:]]+` → `` `[[:space:]`]+` `` at trim boundaries. Eat-own-dog-food regression test: WORKWAY's own Test command wrapped in backticks so the framework's own pre-commit hook runs the backtick-stripping path on every commit — no separate test fixture, the framework IS the regression test. Closes #14.)
- v7.4 (**post-v7.x polish + breaking template removal + two new contract principles**. Origin: a "test review" the user ran on the just-closed v7.x phase to verify hooks/docs/README were consistent — surfaced drift, fixed it, then evolved into the template-functionality-removal question and meta-principles about interaction shape. (a) **Saved-starter template functionality removed entirely**: `/td-flow-init --template <name>` flag, save-as-template path ("save this as a 'X' template"), `templates/<name>/` subdirs, plus the only existing starter `cloudflare-static-assets/` — which had drifted to still use `.td/` post-v7.0 because nobody touched it, proving the cost of frozen-snapshot templates that drift on every framework bump. Modules-on-top (`td-rider` shape: sibling repo + own contract + own commands composing via `@import`) is the better pattern — templates and modules look similar but are different patterns, and templates would have been a parallel weaker version of the modules path. Closes #13 (originally filed by td-rider as a "no rider-astro template work owed" notification; reversed-then-extended in the same session — the user wanted to do the astro template, then realised the cleaner move was pulling the whole template surface). Default `templates/td-flow/` scaffold stays (it's the base every `.td-flow/` is copied into); `templates/CLAUDE.md` (the @import line) stays. (b) **`## Presenting decisions` section added to CLAUDE.md** — numbers (1, 2, 3) the default for all decision lists, each with a short action verb hint (`start / skip / close / comment / fix / promote / verify / ping / drop`); letters (A, B, C) only when nesting (each letter has its own 1/2/3 underneath); plain prose for reports with no decision to make. One-liner reply hint (`Reply: fix/skip`), no multi-line example reply blocks. The mailbox digest format made framework-wide. (c) **`## Principles § Action over questions` extended**: when a directive is ambiguous, pick the most likely reading + state it + execute (don't surface a menu unless the trade-off matters); confirmation is reversibility-gated, not visibility-gated (comments/code/parked issues = just do; force push / deploy / delete remote branches = confirm); don't end turns with offered next steps phrased as questions. (d) **`/td-flow-mailbox` Step 8 + Rules reversibility-gated**: when the directive arrived with reason + shape ("close N — not supported anymore"), draft and execute without a separate confirm round; surface drafts to confirm only when bare or adding material content. Destructive ops (snapshot branch delete local+remote, outbound `reopen` of another project's issue) still confirm. (e) **v7.0 drift misses cleaned**: `templates/cloudflare-static-assets/` still on `.td/` removed; README § Repo layout `td/` → `td-flow/`; `/td-flow-init` save-template path also `.td/`. v7.0 swept install + commands table but missed the layout block + the cloudflare template — smoke didn't catch it because templates layout isn't anchored (worth adding in a future drift-catcher pass). Net −118 lines across the repo today (mostly from cloudflare-static-assets removal); +12 lines of contract investment for the two new principles — the right trade (lean code surface, slightly heavier contract; contract is read once at session start, code surface ships every install).)

## Out of scope (for now)

- Research / context7 deep integration in the rhythm — **decided against** 2026-05-20 (closed #7 as not-planned). The MCP is already wired up and ad-hoc use is already documented in CLAUDE.md. A formal "research phase" would add ceremony for every piece — anti the three-lines-beats-abstraction instinct.
- Subagents for implementation fan-out — **decided against** 2026-05-20 (closed #8 as not-planned). The Agent tool exists in the harness and research fan-out (Explore agents) is already documented; implementation fan-out can't be made fully safe (contract leaks, semantic conflicts past file-level isolation, partial-failure rollback). Coordination cost outweighs speedup for a solo dev where speed isn't the bottleneck. Revisit only with a real concrete case.
- npm package / CLI / Electron studio. Never.
- Claude Code plugin distribution — **evaluated 2026-05-22, ruled out.** The plugin system (marketplace + `/plugin install`) is the native way to ship slash commands + a skill, but it has no mechanism for a per-project standing contract: a plugin can't deliver a file each project `@import`s into its `CLAUDE.md`, and a plugin's own `CLAUDE.md` isn't loaded as project context. That import IS td-flow's core. A hybrid (plugin for the commands, `install.sh` for the contract) would mean two install mechanisms — less lean, not more. Clone + `install.sh` stays.
- Application-code test suite for the framework itself — there's no application code to test. Mechanical sanity checks (syntax, install idempotency, symlink integrity, AWK extractor) are automated as of v5.3 in `scripts/smoke.sh`, wired as the WORKWAY § Local testing Test command so the pre-commit hook runs them every commit. Semantic UAT (does `/td-flow-snapshot` actually work end-to-end?) stays manual, against `mergodon/td-flow-test1`. Drift signals + "Before I commit a piece" ritual remain the self-validation.
