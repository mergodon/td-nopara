@~/.claude/td-flow-contract.md

<!-- The line above imports the canonical td-flow contract — one source of truth,
     kept current by `install.sh` / `/td-flow-refresh`. This file is not a copy.
     Project-specific contract rules, if ever needed, go below this comment;
     all other project content and state lives in `.td/`. -->
