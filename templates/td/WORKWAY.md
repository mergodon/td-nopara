# Way of work

How this project gets tested, verified, and shipped. Section headers are locked. Add and update values as we learn the project. CLAUDE.md routes natural-language statements to the right section here.

## Local testing

Automated checks I run before committing. The pre-commit hook reads `Test command` from this section.

- Test command:    {{test_command}}
- Dev server:      {{dev_command}}
- Local URL:       {{local_url}}
- Pre-ship checklist:
  - [ ] {{test_command}} passes
  - [ ] dev server starts without errors

## Local UAT

Manual verification I (or the user) perform before pushing. For projects where Claude can't fully exercise the UI on its own, this section says what the user needs to do and what I prepare for them.

- Who runs it: {{claude_or_user}}
- What to verify: {{uat_steps_or_none}}
- How: {{uat_how_or_none}}

## Production / Ship

How the change reaches users.

- Live URL:        {{live_url_or_none}}
- Deploy:          {{deploy_command_or_auto_on_push}}
- Smoke after ship: {{smoke_command_or_none}}
- Logs:            {{logs_command_or_url_or_none}}
- Dashboards:      {{dashboards_or_none}}

## Framework specifics

Per-framework awareness. Notes on what each framework brings, how to use it, and any gotchas. Updated when the user tells me about a framework or when I research one (e.g. via context7).

<!-- Examples once populated:

### Laravel + Boost
- MCP server: 15+ Laravel-aware tools available; `.mcp.json` is auto-generated by `boost:install` and gitignored.
- Docs API: 17k Laravel docs, semantic-search accessible.
- Boost regenerates `CLAUDE.md` / `AGENTS.md` / `junie/` on each `boost:install` — gitignored. Our contract is in `CLAUDE.md` (we live with rare overwrites).

### Next.js / Vite / Tampermonkey / etc.
- Stack notes
- Common commands
- Gotchas

-->

(none yet — I'll fill in as we work)

## Notes

Project-specific gotchas, flaky tests, env-only checks, anything that future-me needs to know. Keep it short — git carries the rest.

(none yet)
