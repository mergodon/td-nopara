# Way of work

How this project gets tested, verified, and shipped. Section headers are locked. Add and update values as we learn the project. CLAUDE.md routes natural-language statements to the right section here.

Multi-stack projects (e.g. Python + C++, Laravel + frontend): each section may have multiple subsections — one per stack. Use H3 (`### Python`, `### C++`) for splits. Single-stack projects keep it flat.

## Local testing

Automated checks run before committing. The pre-commit hook reads `Test command` from this section.

- Test command:    {{test_command}}
- Dev server:      {{dev_command}}
- Local URL:       {{local_url}}
- Pre-ship checklist:
  - [ ] {{test_command}} passes
  - [ ] dev server starts without errors

### When local testing isn't possible

Some projects can't fully exercise their behavior locally (Tampermonkey userscripts that need pokercraft.com, integrations that hit live APIs, hardware tests). In that case this section says **the workaround**:

- What can be tested locally: {{partial_local_tests_or_none}}
- What requires the live environment: {{live_only_checks_or_none}}
- The workaround for development: {{workaround_or_none}}

## Local UAT

Manual verification before pushing. For projects where I can't fully exercise the UI on my own, this is what the user does.

- Who runs it: {{claude_or_user}}
- What to verify: {{uat_steps_or_none}}
- How: {{uat_how_or_none}}

## Live

Production / deployed environment. How the change reaches users; how I verify it after pushing.

- Live URL:        {{live_url_or_none}}
- Deploy:          {{deploy_command_or_auto_on_push}}
- Smoke after ship: {{smoke_command_or_none}}
- Logs:            {{logs_command_or_url_or_none}}
- Dashboards:      {{dashboards_or_none}}

## Framework specifics

Per-framework awareness. Notes on what each framework brings, how to use it, and any gotchas. Updated when the user tells me about a framework or when I research one (e.g. via context7).

<!-- Examples once populated:

### Laravel + Boost
- MCP server registered via `.mcp.json` (gitignored, regenerated by `boost:install`).
- Boost Docs API: 17k Laravel docs, semantic-search accessible.
- Boost regenerates `CLAUDE.md` / `AGENTS.md` / `junie/` on each `boost:install` — gitignored.
- If Boost overwrites root `CLAUDE.md`: restore from `~/.claude/td-templates/CLAUDE.md` and tell the user.

### Next.js / Vite / Tampermonkey / Cloudflare Workers / etc.
- Stack notes
- Common commands
- Gotchas

-->

(none yet — I'll fill in as we work)

## Notes

Project-specific gotchas, flaky tests, env-only checks, anything that future-me needs to know. Keep it short — git carries the rest.

(none yet)
