# Backlog

Bigger items noticed but not in scope for the current topic. Append-only. When an item ships, delete the line — git keeps the history.

- 2026-05-21 — /td-health fleet mode: a register of td-flow projects + a `/td-health --all` sweep that runs every project's `.td/health.sh` and stacks the verdict lines into one digest. Scheduled (cron / `/loop`) it becomes a daily fleet health report. The v4.6 verdict-line format was designed for exactly this.
